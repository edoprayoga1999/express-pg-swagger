// const db = require('../configs/database')

const exampleModel = {
    findAll: () => {
        // return new Promise((resolve, reject) => {
        //     db.query('SELECT * FROM example', (error, result) => {
        //         if (error) {
        //             reject(error)
        //         } else {
        //             resolve(result)
        //         }
        //     })
        // })
    }
}

module.exports = exampleModel